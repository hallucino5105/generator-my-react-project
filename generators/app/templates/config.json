{
  "title": "<%= projname %>",

  <% if (framework_type === "React") { %>
  "serve": {
    "public_path": "/",

    "dev": {
      "host": "0.0.0.0",
      "port": <%= defport %>
    },

    "app": {
      "host": "0.0.0.0",
      "port": <%= defport_app %>
    }
  }
  <% } else if (framework_type === "React Native") { %>
  <% } else if (framework_type === "Electron") { %>
  <% } %>
}

